<resources>

    <!-- Base application theme => Geen actionBar: Theme.AppCompat.NoActionBar -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/overalbar_orange</item>
        <item name="colorPrimaryDark">@color/overalbar_orange</item>
        <item name="colorAccent">@color/overalbar_orange</item>
        <item name="android:color">@color/orange</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Om ervoor te zorgen dat er geen enters gedaan kunnen worden in een EditText -->
    <style name="editTextDisableEnter">
        <item name="android:maxLines">1</item>
        <item name="android:lines">1</item>
        <item name="android:singleLine">true</item>
    </style>

</resources>
